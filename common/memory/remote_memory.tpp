#include "remote_memory.h"

/*
template<typename T, uint32_t Addr>
CRemoteMemory<T, Addr>& CRemoteMemory<T, Addr>::operator =(const T& value)
{
  write(value);
  return *this;
}

template<typename T, uint32_t Addr>
T* CRemoteMemory<T, Addr>::operator ->(void)
{
  return &m_data;
}

template<typename T, uint32_t Addr>
CRemoteMemory<T, Addr>::operator T(void)
{
  return read();
}
*/
